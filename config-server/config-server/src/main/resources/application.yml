spring:
  application:
    name: config-server


server:
  port: 8888

cloud:
  config:
    server:
      git:
        uri:
        searchPaths: config-data
        username: ${GIT_USER}
        password: ${GIT_PASSWORD}
